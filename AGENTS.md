## What this is

A local Rust HTTPS MITM proxy for the `claude` CLI (and, now, opencode's `@ai-sdk/google` plus any Anthropic-API client). It does eight specific things and nothing else:

1. **Caches Google OAuth tokens** so `oauth2.googleapis.com/token` round-trips don't happen on every invocation.
2. **Short-circuits Vertex AI heat-up requests** (`max_tokens: 1`, single `"."` user message) so they never burn upstream tokens.
3. **Deduplicates byte-identical concurrent requests** so a duplicate POST results in one upstream call, with both clients receiving the buffered response.
4. **Auto-recovers from expired credentials** — on `invalid_grant` from Google OAuth, opens a browser, runs the consent flow, writes a fresh ADC, and returns a valid token to the client transparently.
5. **Map Local** — for configured `[[map_local]]` rules (URL pattern + method), returns a fixed inline body or local file instead of forwarding upstream. Lets users silence telemetry endpoints, neuter update checks, and replay canned fixtures without burning real API tokens.
6. **Gemini providers** — serves the native Gemini API (`/v1beta/models…`) for opencode's `@ai-sdk/google`, routing each model to the `gemini-cli` or `antigravity` Cloud Code Assist upstream by a **provider prefix** on the model name (`gemini-cli/<model>`, `antigravity/<model>`), with browser `login` for each. Works both as a plain-HTTP origin and via MITM of `generativelanguage.googleapis.com`. Credentials live in `~/.config/claude-proxy/auths/` and are also read from `~/.cli-proxy-api/` (CLIProxyAPI-compatible). See [wiki/gemini-providers.md](wiki/gemini-providers.md).
7. **Anthropic Messages API** — serves `POST /v1/messages` (+ `/v1/messages/count_tokens`) for any Anthropic-API client (Claude Code via `ANTHROPIC_BASE_URL`, the Anthropic SDK), driving the **same** `gemini-cli`/`antigravity` upstreams via the same provider prefix on the body's `model`. It does this by translating Anthropic↔native-Gemini and reusing the Gemini envelope/upstream/SSE layer. Works as a plain-HTTP origin, and via MITM of `api.anthropic.com` **gated on the provider prefix** — unprefixed models fall through to the real Anthropic API so the normal `claude` CLI is untouched. See [wiki/gemini-providers.md](wiki/gemini-providers.md).
8. **OpenAI Chat Completions aggregator** — serves `POST /v1/chat/completions` (origin mode only: `OPENAI_BASE_URL=http://127.0.0.1:7777`) and fans it out to multiple OpenAI-compatible upstreams configured under `[[openai]]` (`name` + `base_url` + optional `api_key`/`headers`). Routing splits the model on the **first** `/`: the head selects the `[[openai]]` provider, the remainder is forwarded verbatim as the upstream `model` (`opengateway/minimax/minimax-m3` → provider `opengateway`, upstream model `minimax/minimax-m3`). **No format translation** — OpenAI in, OpenAI out, a near-pure pipe (rewrite `model`, forward, stream the response back byte-exact). No MITM, no `/v1/models`. Lives in [src/openai/mod.rs](src/openai/mod.rs).

Everything else passes through to `reqwest`, optionally chained via `HTTPS_PROXY` / `config.toml` `upstream_proxy` (e.g. Proxyman, mitmproxy at `localhost:9090`).

## Where to read first

- [wiki/architecture.md](wiki/architecture.md) — full module layout, request flow, OAuth caching state machine, Vertex AI heat-up detection, TLS termination details. **Read this before changing anything in `src/`.**
- [wiki/map-local.md](wiki/map-local.md) — Map Local: config schema, wildcard + method matching, body resolution, Content-Type defaulting matrix, plain-HTTP rationale, error envelope, verification cases.
- [wiki/request-dedup.md](wiki/request-dedup.md) — in-flight request dedup: cache key, state machine, RAII guard semantics, header filtering, validation steps.
- [wiki/auto-reauth.md](wiki/auto-reauth.md) — automatic browser-based OAuth re-auth on `invalid_grant`: detection, spawned-task lifecycle, `REAUTH_PROMISE` gating, RAII safety, validation steps.
- [wiki/gemini-providers.md](wiki/gemini-providers.md) — Gemini API surface for opencode `@ai-sdk/google` **and the Anthropic Messages API surface**: endpoints, provider-prefix routing, envelope transform, credential formats/dirs, `login` flows, config, pipeline placement, the Anthropic↔Gemini translation + MITM prefix gate.
- [wiki/compress.md](wiki/compress.md) — content compression and SmartCrusher: array compaction, token limits, truncation logic, and Vertex AI path resolution.
- [README.md](README.md) — user-facing setup (build, trust CA, set env vars).

## Module map

| File | Responsibility |
| --- | --- |
| [src/main.rs](src/main.rs) | Entry point. Loads config, initializes CA, starts proxy. Subcommands: `start`/`stop`/`restart` and `login {gemini,antigravity}`. Tracing config lives here (`RUST_LOG` honored, default `info,claude_proxy=debug`). |
| [src/config.rs](src/config.rs) | Reads `port` + `upstream_proxy` + `[settings]` + `[[openai]]` from `config.toml` (and exposes `resolve_port` for the CLI > config > default port precedence) (CLI `--config`, then `./config.toml`, then `~/.config/claude-proxy/config.toml`). Deliberately does **not** read `HTTPS_PROXY` — that var is for clients pointing at us; reading it here would chain the proxy through itself. |
| [src/certs.rs](src/certs.rs) | Root CA at `~/Library/Application Support/claude-proxy/ca.{crt,key}` (rcgen 0.11). Dynamic leaf certs per intercepted host. |
| [src/proxy.rs](src/proxy.rs) | Hyper server, `CONNECT` handling, TLS termination, request routing, upstream forwarding. Defines `ProxyBody` (boxed body — supports both buffered `Full` and SSE streams) and the `full_body`/`box_full` helpers; calls `gemini::try_handle` then `gemini::anthropic::try_handle` in both branches after Map Local (the Anthropic MITM block is gated on `host == api.anthropic.com` + provider prefix), plus `openai::try_handle` in the plain-HTTP branch only (origin mode). |
| [src/openai/](src/openai/) | OpenAI Chat Completions aggregator (`mod.rs`, single file). `is_chat_completions_path`, dynamic-prefix `split_model` (first `/`-segment → configured provider name, remainder = upstream model), `try_handle`. No translation: rewrites `model`, POSTs to `{base_url}/chat/completions` via the shared proxy client (config `api_key` Bearer wins, else forwards the client's incoming `Authorization`; config `headers` added), and pipes the response — buffered for non-stream, a disconnect-safe raw-byte pump (`stream_passthrough`, same `tx.closed()` contract as `gemini::provider::stream_sse`) for stream. OpenAI error envelope `{"error":{message,type,code}}`. |
| [src/interceptors.rs](src/interceptors.rs) | OAuth token cache (disk + in-flight dedup), Vertex AI heat-up detection, generic request-dedup machinery (`BufferedResponse`, `REQUEST_PROMISES`, `RequestPrimaryGuard`, `handle_dedup_request`), and Map Local (`match_map_local`, `build_map_local_response`, hand-rolled `wildcard_match` + MIME helper). |
| [src/reauth.rs](src/reauth.rs) | Browser OAuth flow triggered on `invalid_grant`. `REAUTH_PROMISE` gate, `ReauthGuard` RAII, ADC writer, non-proxied token-exchange client; uses the shared callback/percent helpers in `oauth_util`. See [wiki/auto-reauth.md](wiki/auto-reauth.md). |
| [src/oauth_util.rs](src/oauth_util.rs) | Shared browser-OAuth helpers: localhost callback server (`accept_oauth_callback`), `percent_encode`/`percent_decode`, `open_browser`. Used by `reauth.rs` and `login.rs`. |
| [src/login.rs](src/login.rs) | `login gemini` / `login antigravity` CLI flows: browser consent → code exchange → email + project (`loadCodeAssist`/`onboardUser`) → write credential file. All HTTP uses a `no_proxy()` client. |
| [src/gemini/](src/gemini/) | Gemini + Anthropic API surfaces over the Cloud Code Assist upstreams. `mod.rs` (`/v1beta` `try_handle`, routing, error envelopes, `GeminiState`), `models.rs` (prefix router `split_model` + the `/v1beta/models` listing, fetched live per provider — gemini-cli via `retrieveUserQuota`, antigravity via `fetchAvailableModels` — with an optional `[settings] models_file` catalog as fallback/enrichment; no embedded/remote catalog), `creds.rs` (discover/parse/refresh both cred formats across both dirs; provider OAuth client constants), `translate.rs` (gemini↔gemini-cli/antigravity envelope wrap/unwrap), `provider.rs` (upstream POST + generic disconnect-safe `stream_sse` pump + gemini SSE), `schema_clean.rs` (antigravity tool-schema cleaner — rename `parametersJsonSchema`→`parameters` + strip/flatten unsupported JSON-Schema keywords; ported from CLIProxyAPI's antigravity executor, applied in `gemini_to_antigravity`), `anthropic.rs` (`/v1/messages` + `/count_tokens` routing, model-prefix MITM gate, Anthropic error envelopes), `anthropic_translate.rs` (Anthropic↔native-Gemini request/response + streaming state machine + tool-name sanitize/restore). See [wiki/gemini-providers.md](wiki/gemini-providers.md). |
| [src/daemon.rs](src/daemon.rs) | `start` / `stop` / `restart` subcommands. PID files at `~/.config/claude-proxy/pids/{port}.pid`, logs at `~/.config/claude-proxy/log/{epoch}.log`. |

## Key invariants — do not break

- **`PrimaryGuard` is RAII.** The primary OAuth fetcher owns a `PrimaryGuard` whose `Drop` impl removes the in-flight entry from `TOKEN_PROMISES`. This is what prevents secondary waiters from hanging forever if the primary's task is cancelled (e.g., client disconnect). Resolve via `guard.resolve(token)` for normal completion; never call `mem::forget` on it; never re-introduce the old `resolve_token_promise` free function.
- **`RequestPrimaryGuard` is RAII (same pattern).** Mirrors `PrimaryGuard` for the generic request-dedup map (`REQUEST_PROMISES`). Always resolve via `guard.resolve(Some(buf))` on 2xx or `guard.resolve(None)` on non-2xx / upstream error so secondaries fall through to native fetch. The `Err` arm of the upstream send must resolve too — leaking a guard there strands secondaries until Drop fires. On the **routed Anthropic path** the guard is resolved by `proxy::record_for_dedup` instead: buffered responses are collected and resolved inline; streams are wrapped in `RecordingBody`, which resolves at EOF. `RecordingBody` deliberately does **not** override `is_end_stream`/`size_hint` — the defaults are what keep hyper polling to `Ready(None)`, and that final poll is the only thing that resolves the promise. On client disconnect the body is dropped with the guard still held, so the same `Drop` eviction applies.
- **`ReauthGuard` is RAII and lives inside a `tokio::spawn`-ed task.** The OAuth browser flow must outlive the request handler that triggered it — clients commonly time out during the 5-minute browser window, and the next retry depends on finding the in-progress flow via `REAUTH_PROMISE`. Keep `run_oauth_flow()` spawned, not inline. See [wiki/auto-reauth.md](wiki/auto-reauth.md).
- **The token-exchange POST in `reauth.rs` uses a `no_proxy()` `reqwest::Client`.** The proxy's main client may chain through `HTTPS_PROXY` (which is us). A proxied exchange would loop back through the very interceptor that triggered re-auth. Don't share the proxy's `reqwest::Client` with `reauth.rs`. **Same rule applies to `login.rs` (all calls) and `gemini::creds` token refresh** — `login` runs in the user's shell where `HTTPS_PROXY` points at us, so a default client would self-loop. The Gemini *upstream* generate/stream call deliberately reuses the proxy's main client so it benefits from `upstream_proxy` chaining (Proxyman); that's not a loop because cloudcode-pa ≠ us. **The OpenAI aggregator's upstream POST reuses the proxy's main client for the same reason** (the configured `base_url` ≠ us), so don't switch it to a `no_proxy()` client.
- **Pipeline order: Map Local → Gemini → Anthropic → OAuth → Vertex heat-up → dedup → upstream forward.** Map Local sits at the very top so a mapped URL fully bypasses everything else (a user mapping the OAuth endpoint to a fixture wants the fixture, not the disk-cached token). Gemini then Anthropic routing come next so a `/v1beta` or `/v1/messages` request is fully served (translated to the provider upstream) and never touches the OAuth-token cache or the Vertex heat-up check, which are specific to the `claude` CLI's traffic. **Dedup is the one exception: the routed Anthropic path registers in `REQUEST_PROMISES` itself** (both transports), because the early return jumps over the shared dedup block below and Claude Code fires byte-identical concurrent `/v1/messages` requests for session-title generation — one provider generation per duplicate otherwise. The two uses share the map and key safely: routability is a pure function of the body, so a routed and a forwarded request can never collide. Heat-ups must short-circuit before the dedup map is touched, otherwise a heat-up-shaped body would inflate the in-flight set with synthetic-response candidates. Keep the order in [src/proxy.rs::handle_intercepted_request](src/proxy.rs). The plain-HTTP branch of `handle_request` runs Map Local → Gemini → Anthropic → OpenAI (origin mode); non-mapped, non-Gemini, non-Anthropic, non-OpenAI plain HTTP still 500s. The OpenAI aggregator (`/v1/chat/completions`) is plain-HTTP/origin-only — it is **not** wired into the MITM branch at all (no `api.openai.com` interception), so there's no MITM safety gate to maintain for it. In the MITM branch, Gemini routing is gated on `host == generativelanguage.googleapis.com`, and **Anthropic routing is gated on `host == api.anthropic.com` AND the body's `model` being routable** (`anthropic::routed_provider`) — this is the safety crux: a request whose model is neither provider-prefixed nor explicitly listed in `[anthropic_model_map]` falls through to the normal upstream forward (the real Anthropic API), so the `claude` CLI is never hijacked. `[anthropic_model_map]` (`config.rs`, empty by default) is the one deliberate, narrow exception — an exact-match config that lets a specific real Anthropic model name (e.g. `claude-sonnet-5`) be redirected to a provider-prefixed target over **either** transport (this is the feature's actual purpose: redirecting the real `claude` CLI's MITM'd traffic); it does not weaken the gate for any model not explicitly listed. See [wiki/gemini-providers.md](wiki/gemini-providers.md).
- **All handlers return `Response<ProxyBody>` (boxed body).** `ProxyBody = BoxBody<Bytes, std::io::Error>`. Buffered responses use `full_body(bytes)`; the interceptor helpers still return `Response<Full<Bytes>>` and are re-boxed at the call site with `box_full(...)`. The Gemini and Anthropic SSE paths are the true streams — both built on the one disconnect-safe `provider::stream_sse` pump (keep the `biased` `tokio::select!` on `tx.closed()`). Don't revert the handler signatures to `Full<Bytes>` — streaming depends on the boxed type.
- **Dedup cache key is `format!("{} {}\n{}", method, url, body_str)`.** Method + URL prevent unrelated empty-body GETs from false-deduping; body is the discriminator. If you ever change this, update [wiki/request-dedup.md](wiki/request-dedup.md).
- **Cache key is the request body.** `~/.config/gcloud/application_default_credentials_access_token.json` stores the original `request_body` alongside the token; cache hits require an exact body match. If a caller's body varies between calls (e.g., JWT-bearer flows where `iat`/`exp` change), the cache will miss every time. If you need to support that case, change the key strategy — don't paper over it.
- **Upstream forwarding strips three headers**: `host`, `accept-encoding`, `content-length`. Keep that list in sync if you add header logic; messing with `accept-encoding` will silently break OAuth JSON parsing (we don't enable reqwest's `gzip` feature).
- **`danger_accept_invalid_certs(true)`** on the upstream `reqwest::Client` is intentional — it lets us chain through Proxyman/mitmproxy. Don't remove it.
- **Listening port defaults to `127.0.0.1:7777`** ([`proxy::DEFAULT_PORT`](src/proxy.rs)) but is configurable. Precedence: CLI `--port` > config `port` > `DEFAULT_PORT`, resolved by [`config::resolve_port`](src/config.rs). The daemon `start` loads config *before* binding so the config `port` feeds the bind (and reuses that config in the forked child); `restart` resolves the same way for its socket-release probe. Keep all three call sites (`main::run_foreground`, `daemon::start`, `daemon::restart`) going through `resolve_port` so precedence stays consistent.

## Build and test

```bash
cargo build              # debug
cargo build --release    # what users actually run
RUST_LOG=claude_proxy=debug,reqwest=debug,hyper=info target/release/claude-proxy
```

There are no automated tests yet. Validate changes by:
1. Building.
2. Running the proxy.
3. Running `claude` with `HTTPS_PROXY=http://127.0.0.1:7777` and `NODE_EXTRA_CA_CERTS=~/Library/Application\ Support/claude-proxy/ca.crt`.
4. Watching logs for the expected `Cache hit on disk for token` / `Intercepted Vertex AI heat-up request` lines on subsequent invocations.
5. For dedup: fire two byte-identical concurrent requests (e.g. `sed '1 s/^curl /curl -k /' refs/1.sh | bash & sed '1 s/^curl /curl -k /' refs/2.sh | bash & wait`) and confirm the log shows one `We are the primary fetcher` plus one `Waiting on primary in-flight request` followed by `Received response from primary in-flight request`.

## Things to be careful about

- **rcgen is pinned to 0.11.** 0.14 has a different API (no `Certificate::from_params`, `from_ca_cert_pem`); reverting was deliberate. Don't bump it without rewriting [src/certs.rs](src/certs.rs).
- **rand is 0.10**, which uses `RngExt::sample_iter` (not the older `Rng::sample_iter`). Keep the `RngExt` import in [src/interceptors.rs](src/interceptors.rs).
- **Don't add features the proxy doesn't need.** This codebase is intentionally narrow — it intercepts a handful of endpoints, serves the Gemini + Anthropic APIs, and forwards the rest. Resist adding "while we're here" abstractions. The Gemini layer deliberately ports only the Gemini↔provider transform; the Anthropic layer adds **only** the Anthropic↔native-Gemini boundary and reuses everything downstream (it does **not** re-implement the dedicated `antigravity/claude` signature path — claude-on-antigravity rides the gemini envelope + `VALIDATED` toolConfig tweak + the antigravity tool-schema cleaner in `schema_clean.rs`, same as the `/v1beta` surface). **Gotcha:** for antigravity, tool schemas must be sent as `functionDeclarations[].parameters` (not `parametersJsonSchema`) and cleaned of unsupported JSON-Schema keywords — that step lives in CLIProxyAPI's *executor*, not its translator, so it's easy to miss; without it the Vertex Anthropic backend returns `tools.0.custom.input_schema: Field required`. Deferred items (`stop_sequences`, the dedicated `antigravity/claude` signature path, etc.) are listed in [wiki/gemini-providers.md](wiki/gemini-providers.md). (`max_tokens`→`maxOutputTokens` **is** mapped — the `-thinking` models need `max_tokens > thinking.budget_tokens`.)
- **The `GET /v1beta/models` listing is fetched live per provider** — gemini-cli via `cloudcode-pa …:retrieveUserQuota`, antigravity via `daily-cloudcode-pa …:fetchAvailableModels` — each using the same hardcoded client headers as the generate path. There is **no embedded and no remote catalog** (both removed); an optional `[settings] models_file` (`models.json` on disk) is the only fallback/enrichment source, used per-provider when its live fetch fails. The listing never gates routing (prefix-based). If you ever add a non-`.rs` runtime asset under `src/`, add it to [Cargo.toml](Cargo.toml)'s `include` list or `cargo publish` will exclude it.
- **README.md must use full GitHub URLs for `wiki/` links.** The `include` list in [Cargo.toml](Cargo.toml) ships `README.md` to crates.io but **not** the `wiki/` directory — relative links like `[wiki/auto-reauth.md](wiki/auto-reauth.md)` render as broken links on the crates.io page. Always link wiki pages from README.md as `https://github.com/aero/claude-proxy/blob/master/wiki/<file>.md`. Internal docs (AGENTS.md, CLAUDE.md, GEMINI.md, other wiki pages) can keep relative paths since they're never published to crates.io.

## Intentionally trusting

claude-proxy is a single-user MITM proxy on `127.0.0.1` that assumes a trusted local environment. Security findings whose threat model requires a *local* process/user the operator doesn't control are intentionally **not** fixed — the only realistic "attacker" is the operator themselves chaining another proxy downstream on purpose.

Concrete decisions in this class (all deliberate, don't re-flag or "harden"):
- OAuth `state` is generated and sent in the login auth URL but **not** validated on the callback (CSRF / code-injection on the loopback callback port). Declined 2026-05.
- `danger_accept_invalid_certs(true)` on the upstream client (to chain through Proxyman/mitmproxy).
- Inbound API keys (`x-goog-api-key`, Anthropic key) are ignored — real auth is the stored OAuth credential.
- Account email is logged to local logs across the Gemini/creds/login paths.

**Why:** trusted-local-network assumption; a local attacker already has the operator's privileges, so these guards add cost for no real benefit here.
